[project]
name = "catalog-scrap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yenaro Joel Noa Camino", email = "ynoacamino@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pdfplumber>=0.11.10",
]

[project.scripts]
catalog-scrap = "catalog_scrap:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
